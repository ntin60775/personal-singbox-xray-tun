# Карточка задачи TASK-2026-0005

## Паспорт

| Поле | Значение |
|------|----------|
| ID задачи | `TASK-2026-0005` |
| Parent ID | `—` |
| Уровень вложенности | `0` |
| Ключ в путях | `TASK-2026-0005` |
| Технический ключ для новых именуемых сущностей | `—` |
| Краткое имя | `project-structure-cleanup` |
| Статус | `завершена` |
| Приоритет | `средний` |
| Ответственный | `не назначен` |
| Ветка | `—` |
| Дата создания | `2026-03-27` |
| Дата обновления | `2026-04-03` |

## Цель

Отделить пользовательские точки входа, внутреннюю реализацию и инженерную документацию так, чтобы bundle оставался переносимым и не ломал текущий сценарий запуска через CLI и GUI.

Исторический ожидаемый результат на момент исходного legacy-плана: корень репозитория содержит только публичные entrypoint-скрипты, operator-managed конфиги, `logs/`, `plans/` и верхнеуровневую документацию, а внутренняя реализация и вспомогательные артефакты разнесены по специализированным каталогам без изменения пользовательского контракта.

## Подсказка по статусу

Использовать только одно из значений:

- `черновик`
- `готова к работе`
- `в работе`
- `на проверке`
- `ждёт пользователя`
- `заблокирована`
- `завершена`
- `отменена`

## Границы

### Входит

- историческое содержимое legacy-плана, сохранённого в `artifacts/legacy-plan.md`;
- адаптация записи под task-centric структуру `knowledge/tasks/`;
- сохранение исходного Markdown в `artifacts/legacy-plan.md`.

### Не входит

- новая реализация задачи в рамках миграции;
- редактирование исторического смысла legacy-плана;
- удаление или перепридумывание исходного контекста.

## Контекст

- источник постановки: исходная формулировка сохранена в `artifacts/legacy-plan.md`; исходный путь зафиксирован в карте миграции.
- связанная бизнес-область: legacy-планы проекта
- ограничения и зависимости: миграция выполняется без потери исходного Markdown и без сохранения удалённого legacy-контура как рабочего процесса
- основной контекст сессии: `историческая миграция`

## Затронутые области

| Область | Что меняется |
|---------|--------------|
| Код / сервисы | Историческое описание сохранено в `artifacts/legacy-plan.md` |
| Конфигурация / схема данных / именуемые сущности | Историческое описание сохранено в `artifacts/legacy-plan.md` |
| Интерфейсы / формы / страницы | Историческое описание сохранено в `artifacts/legacy-plan.md` |
| Интеграции / обмены | Историческое описание сохранено в `artifacts/legacy-plan.md` |
| Документация | Созданы `task.md` и `plan.md`, а legacy-план сохранён как артефакт |

## Связанные материалы

- основной каталог задачи: `knowledge/tasks/TASK-2026-0005-project-structure-cleanup/`
- файл плана: `plan.md`
- пользовательские материалы: `artifacts/legacy-plan.md`
- связанные коммиты / PR / ветки: `—`
- связанные операции в `knowledge/operations/`, если есть: `—`

## Текущий этап

Историческая задача была завершена до миграции из legacy-контура и перенесена в knowledge-систему как архивная, но доступная запись.

## Стратегия проверки

### Покрывается кодом или тестами

- см. раздел `Проверки` в `plan.md`;
- при необходимости сверить с `artifacts/legacy-plan.md`.

### Остаётся на ручную проверку

- сверить migrated-каталог с `artifacts/legacy-plan.md`; исходный путь при необходимости взять из `knowledge/tasks/TASK-2026-0002-plans-to-knowledge-migration/artifacts/legacy/legacy-path-map.md`.

## Критерии готовности

- каталог `knowledge/tasks/TASK-2026-0005-project-structure-cleanup/` содержит адаптированные `task.md` и `plan.md`;
- исходный Markdown сохранён в `artifacts/legacy-plan.md`;
- дальнейшая работа по этой теме, если понадобится, продолжается только из knowledge-системы.

## Итог

Реализован новый layout bundle: shell-общие path-хелперы вынесены в `lib/subvost-common.sh`, реальные реализации перенесены в `libexec/`, Python backend — в `gui/gui_server.py`, исследовательский отчёт — в `docs/research/deep-research-report.md`, SVG-иконка — в `assets/`. Корневые `run/stop/capture/open/install` теперь работают как thin wrapper'ы, которые вычисляют корень bundle по собственному пути, переопределяют `SUBVOST_PROJECT_ROOT` и `exec`-ят internal-реализацию.

GUI-контракт обновлён: `gui/gui_server.py` теперь резолвит action-path'ы только к публичным wrapper'ам в корне, публикует `project_root` и `bundle_identity` в `/api/status`, а `libexec/open-subvost-gui.sh` переиспользует backend только при совпадении `gui_version` и `project_root`, иначе инициирует restart через `pkexec` и `libexec/start-gui-backend-root.sh`. `subvost-xray-tun.desktop` переведён на self-locating `Exec` с `%k`, без старых absolute path.

Статические проверки выполнены: `bash -n *.sh`, `bash -n libexec/*.sh`, `bash -n lib/*.sh`, `python3 -m py_compile gui/gui_server.py`, `desktop-file-validate subvost-xray-tun.desktop`, а также поиск по репозиторию на старые absolute path и прямые зависимости от устаревшего flat-layout. Остаточные риски: в текущем окружении не выполнены ручные smoke-проверки с `sudo`, `pkexec`, переносом bundle в другой путь и запуском `.desktop` из реального desktop-session; их нужно провести отдельно на целевой машине.

Исторический Markdown сохранён в `artifacts/legacy-plan.md`; исходный путь legacy-контура зафиксирован в `knowledge/tasks/TASK-2026-0002-plans-to-knowledge-migration/artifacts/legacy/legacy-path-map.md`.
