# Карточка задачи TASK-2026-0028

## Паспорт

| Поле | Значение |
|------|----------|
| ID задачи | `TASK-2026-0028` |
| Parent ID | `—` |
| Уровень вложенности | `0` |
| Ключ в путях | `TASK-2026-0028` |
| Технический ключ для новых именуемых сущностей | `—` |
| Краткое имя | `ui-runtime-mode-polish` |
| Статус | `завершена` |
| Приоритет | `средний` |
| Ответственный | `не назначен` |
| Ветка | `—` |
| Дата создания | `2026-04-01` |
| Дата обновления | `2026-04-02` |

## Цель

Убрать двусмысленность в основном пользовательском сценарии `добавил подписку -> увидел результат -> выбрал источник runtime -> запустил`, чтобы web UI был понятен без чтения логов и без знания внутренних файлов bundle.

## Подсказка по статусу

Использовать только одно из значений:

- `черновик`
- `готова к работе`
- `в работе`
- `на проверке`
- `ждёт пользователя`
- `заблокирована`
- `завершена`
- `отменена`

## Границы

### Входит

- историческое содержимое legacy-плана, сохранённого в `artifacts/legacy-plan.md`;
- адаптация записи под task-centric структуру `knowledge/tasks/`;
- сохранение исходного Markdown в `artifacts/legacy-plan.md`.

### Не входит

- новая реализация задачи в рамках миграции;
- редактирование исторического смысла legacy-плана;
- удаление или перепридумывание исходного контекста.

## Контекст

- источник постановки: запрос пользователя отшлифовать backend и web UI, сделать ясным добавление подписки и добавить явное ручное переключение на встроенный config
- связанная бизнес-область: legacy-планы проекта
- ограничения и зависимости: миграция выполняется без потери исходного Markdown и без сохранения удалённого legacy-контура как рабочего процесса
- основной контекст сессии: `историческая миграция`

## Затронутые области

| Область | Что меняется |
|---------|--------------|
| Код / сервисы | Историческое описание сохранено в `artifacts/legacy-plan.md` |
| Конфигурация / схема данных / именуемые сущности | Историческое описание сохранено в `artifacts/legacy-plan.md` |
| Интерфейсы / формы / страницы | Историческое описание сохранено в `artifacts/legacy-plan.md` |
| Интеграции / обмены | Историческое описание сохранено в `artifacts/legacy-plan.md` |
| Документация | Созданы `task.md` и `plan.md`, а legacy-план сохранён как артефакт |

## Связанные материалы

- основной каталог задачи: `knowledge/tasks/TASK-2026-0028-ui-runtime-mode-polish/`
- файл плана: `plan.md`
- пользовательские материалы: `artifacts/legacy-plan.md`
- связанные коммиты / PR / ветки: `—`
- связанные операции в `knowledge/operations/`, если есть: `—`

## Текущий этап

Историческая задача была завершена до миграции из legacy-контура и перенесена в knowledge-систему как архивная, но доступная запись.

## Стратегия проверки

### Покрывается кодом или тестами

- см. раздел `Проверки` в `plan.md`;
- при необходимости сверить с `artifacts/legacy-plan.md`.

### Остаётся на ручную проверку

- сверить migrated-каталог с `artifacts/legacy-plan.md`; исходный путь при необходимости взять из `knowledge/tasks/TASK-2026-0002-plans-to-knowledge-migration/artifacts/legacy/legacy-path-map.md`.

## Критерии готовности

- каталог `knowledge/tasks/TASK-2026-0028-ui-runtime-mode-polish/` содержит адаптированные `task.md` и `plan.md`;
- исходный Markdown сохранён в `artifacts/legacy-plan.md`;
- дальнейшая работа по этой теме, если понадобится, продолжается только из knowledge-системы.

## Итог

- В backend/store добавлен явный `runtime_preference` с режимами `store` и `builtin`, а shell-слой теперь понимает этот выбор и отражает его в runtime-state без автозапуска стека.
- Web UI перестроен вокруг более ясного сценария: появился заметный переключатель источника runtime, локальный feedback по подпискам и более явные тексты о том, что используется сейчас и что пойдёт в следующий старт.
- На реальной ссылке `https://example.com/subscription` подтвержден безопасный fetch и парсинг без конфликта с текущим продовым VPN: upstream отдал `2` валидные строки в `base64`, после dedup сохранился `1` уникальный узел и это теперь явно показывается в ответе backend/UI вместе с числом схлопнутых дублей.
- Подтверждены безопасные проверки без запуска локального `Старт/Стоп`: unit-тесты, shell syntax-check, `py_compile`, HTTP smoke локального GUI backend, добавление подписки и ручное переключение `builtin/store`.
- Остаточный риск: автоматизированный browser-level smoke через Playwright в этой сессии не выполнен, потому что локальный `playwright-cli` через `npx` зависал на bootstrap; фактическая выдача HTML и HTTP API нового UI/backend подтверждена напрямую через локальный backend.

Исторический Markdown сохранён в `artifacts/legacy-plan.md`; исходный путь legacy-контура зафиксирован в `knowledge/tasks/TASK-2026-0002-plans-to-knowledge-migration/artifacts/legacy/legacy-path-map.md`.
