# План задачи TASK-2026-0030

## Правило

Для задачи существует только один файл плана: `plan.md`.
Если задача декомпозируется, каждая подзадача получает свой собственный `plan.md` внутри своей папки.

## Паспорт плана

| Поле | Значение |
|------|----------|
| ID задачи | `TASK-2026-0030` |
| Parent ID | `—` |
| Версия плана | `1` |
| Дата обновления | `2026-04-02` |

## Цель

Исправить конкретные UX-проблемы текущего Clash-кандидата: убрать лишнюю верхнюю плашку, развести sidebar и основную рабочую область, уплотнить верхнюю панель и заменить пустое место полезной сводкой.

## Границы

### Входит

- исторический объём legacy-плана, сохранённого в `artifacts/legacy-plan.md`;
- сохранение исходного Markdown в `artifacts/legacy-plan.md`;
- приведение записи к task-centric структуре без потери контекста.

### Не входит

- новая реализация темы во время миграции;
- изменение исторического статуса legacy-задачи.

## Планируемые изменения

### Код

- содержание legacy-плана сохранено без переписывания; детали см. в историческом блоке ниже и в `artifacts/legacy-plan.md`.

### Конфигурация / схема данных / именуемые сущности

- при наличии изменений они описаны в историческом блоке ниже и в `artifacts/legacy-plan.md`.

### Документация

- task-centric карточка и план созданы в `knowledge/tasks/TASK-2026-0030-clash-candidate-feedback-round-1/`.

## Риски и зависимости

- legacy-план мог использовать ссылки удалённого legacy-контура; они сохраняются как исторический контекст внутри `artifacts/legacy-plan.md`.
- продолжение этой темы, если оно потребуется, должно идти уже из knowledge-системы, а не через восстановление отдельного legacy-контура.

## Проверки

### Что можно проверить кодом или тестами

- при наличии команд и автоматизируемых проверок см. исторический блок `Проверки` ниже.

### Что остаётся на ручную проверку

- сверить migrated-каталог с `artifacts/legacy-plan.md`.

## Шаги

- [x] Перенести исходный legacy-план в task-centric структуру
- [x] Сохранить исходный Markdown в `artifacts/legacy-plan.md`
- [x] Историческое состояние задачи зафиксировано и не требует дополнительной миграционной декомпозиции

## Критерии завершения

- Историческая задача уже завершена; текущий критерий миграции — сохранить контекст без потери деталей.

## Источник legacy-плана

- текущий артефакт: `artifacts/legacy-plan.md`
- карта миграции исходного пути: `knowledge/tasks/TASK-2026-0002-plans-to-knowledge-migration/artifacts/legacy/legacy-path-map.md`
- исходный статус: `done`
- исходный заголовок: `Доработка Clash-кандидата по замечаниям со скриншота`

## Исторический контекст

### Изменения

- Убрать верхнюю review-плашку, которая отъедает высоту и не несёт ценности внутри самого интерфейса.
- Увеличить визуальный зазор между sidebar и основной рабочей областью, чтобы панель не «наезжала» на боковое меню.
- Уменьшить высоту верхнего header-блока рабочей области.
- Добавить в верхний блок компактные полезные метрики вместо пустого пространства.
- Сохранить fullscreen-поведение без вертикального скролла для развёрнутого desktop-окна.

## Исторические проверки

- `python3 -m py_compile gui/gui_server.py`
- `python3 -m unittest tests.test_gui_server`
- Локальный HTTP smoke:
  - открыть `/design-review`;
  - проверить, что верхняя review-плашка убрана;
  - проверить, что clash-кандидат остаётся основным единственным экраном.

## Исторические допущения

- Замечание «это убрать» относится к верхней review-плашке над самим интерфейсом, а не к внутреннему sidebar-блоку бренда.
- Сценарий fullscreen по-прежнему приоритетнее, чем поведение в узком окне.

## Исторический итог

- Верхняя review-плашка полностью удалена, чтобы не тратить вертикальную высоту на внешний служебный слой.
- Между sidebar и рабочей областью добавлен явный зазор через внутренний `gap` у основного окна и пересобранные отступы workspace.
- Верхний header рабочей области уменьшен по высоте и дополнен полезной сводкой:
  - рабочие узлы;
  - свежие подписки;
  - состояние стека.
- Fullscreen-режим для развёрнутого окна сохранён: основная поверхность по-прежнему собирается в высоту viewport без отдельной вертикальной ленты страницы.
- Подтверждено:
  - `python3 -m py_compile gui/gui_server.py`;
  - `python3 -m unittest tests.test_gui_server`;
  - локальный HTTP smoke на `/design-review` с проверкой новых верхних метрик.
