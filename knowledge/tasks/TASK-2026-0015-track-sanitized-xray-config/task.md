# Карточка задачи TASK-2026-0015

## Паспорт

| Поле | Значение |
|------|----------|
| ID задачи | `TASK-2026-0015` |
| Parent ID | `—` |
| Уровень вложенности | `0` |
| Ключ в путях | `TASK-2026-0015` |
| Технический ключ для новых именуемых сущностей | `—` |
| Краткое имя | `track-sanitized-xray-config` |
| Статус | `завершена` |
| Приоритет | `средний` |
| Ответственный | `не назначен` |
| Ветка | `—` |
| Дата создания | `2026-03-29` |
| Дата обновления | `2026-04-02` |

## Цель

Убрать `xray-tun-subvost.json` из `.gitignore` и добавить в репозиторий sanitized-версию файла, чтобы структура проекта оставалась полной, но реальные `id`, `PublicKey` и `shortId` не хранились в git.

Ожидаемый результат: `xray-tun-subvost.json` появляется в репозитории как шаблон с актуальными несекретными параметрами узла и явными placeholders для `id`, `publicKey` и `shortId`, а `README.md` и `AGENTS.md` сразу подсказывают, какие поля оператор и ИИ должны дозаполнить локально.

## Подсказка по статусу

Использовать только одно из значений:

- `черновик`
- `готова к работе`
- `в работе`
- `на проверке`
- `ждёт пользователя`
- `заблокирована`
- `завершена`
- `отменена`

## Границы

### Входит

- историческое содержимое legacy-плана, сохранённого в `artifacts/legacy-plan.md`;
- адаптация записи под task-centric структуру `knowledge/tasks/`;
- сохранение исходного Markdown в `artifacts/legacy-plan.md`.

### Не входит

- новая реализация задачи в рамках миграции;
- редактирование исторического смысла legacy-плана;
- удаление или перепридумывание исходного контекста.

## Контекст

- источник постановки: запрос пользователя сделать `xray-tun-subvost.json` частью git-репозитория без публикации реальных `id`, `PublicKey` и `shortId`
- связанная бизнес-область: legacy-планы проекта
- ограничения и зависимости: миграция выполняется без потери исходного Markdown и без сохранения удалённого legacy-контура как рабочего процесса
- основной контекст сессии: `историческая миграция`

## Затронутые области

| Область | Что меняется |
|---------|--------------|
| Код / сервисы | Историческое описание сохранено в `artifacts/legacy-plan.md` |
| Конфигурация / схема данных / именуемые сущности | Историческое описание сохранено в `artifacts/legacy-plan.md` |
| Интерфейсы / формы / страницы | Историческое описание сохранено в `artifacts/legacy-plan.md` |
| Интеграции / обмены | Историческое описание сохранено в `artifacts/legacy-plan.md` |
| Документация | Созданы `task.md` и `plan.md`, а legacy-план сохранён как артефакт |

## Связанные материалы

- основной каталог задачи: `knowledge/tasks/TASK-2026-0015-track-sanitized-xray-config/`
- файл плана: `plan.md`
- пользовательские материалы: `artifacts/legacy-plan.md`
- связанные коммиты / PR / ветки: `—`
- связанные операции в `knowledge/operations/`, если есть: `—`

## Текущий этап

Историческая задача была завершена до миграции из legacy-контура и перенесена в knowledge-систему как архивная, но доступная запись.

## Стратегия проверки

### Покрывается кодом или тестами

- см. раздел `Проверки` в `plan.md`;
- при необходимости сверить с `artifacts/legacy-plan.md`.

### Остаётся на ручную проверку

- сверить migrated-каталог с `artifacts/legacy-plan.md`; исходный путь при необходимости взять из `knowledge/tasks/TASK-2026-0002-plans-to-knowledge-migration/artifacts/legacy/legacy-path-map.md`.

## Критерии готовности

- каталог `knowledge/tasks/TASK-2026-0015-track-sanitized-xray-config/` содержит адаптированные `task.md` и `plan.md`;
- исходный Markdown сохранён в `artifacts/legacy-plan.md`;
- дальнейшая работа по этой теме, если понадобится, продолжается только из knowledge-системы.

## Итог

`/xray-tun-subvost.json` удалён из `.gitignore`, поэтому файл теперь виден git и может жить в репозитории как часть структуры bundle.

В корень проекта добавлен `xray-tun-subvost.json` с актуальными несекретными параметрами рабочего узла: адресом `203.0.113.10`, `serverName` и xhttp-параметрами. Поля `id`, `publicKey` и `shortId` намеренно заменены на placeholders `REPLACE_WITH_REALITY_UUID`, `REPLACE_WITH_REALITY_PUBLIC_KEY` и `REPLACE_WITH_REALITY_SHORT_ID`, поэтому перед реальным запуском bundle оператор должен подставить локальные значения.

`README.md` и `AGENTS.md` дополнительно фиксируют точные JSON-пути и имена placeholders, чтобы и пользователь, и ИИ не путали tracked-шаблон с локальным runtime-конфигом.

Проверки завершены так: `python3 -m json.tool xray-tun-subvost.json` проходит, `git check-ignore -v xray-tun-subvost.json` возвращает `exit=1` и подтверждает, что файл больше не игнорируется, а `git status --short` показывает ожидаемые изменения `.gitignore`, нового `xray-tun-subvost.json`, документации и плана. Остаточный риск: tracked-файл без реальных `id`, `PublicKey` и `shortId` не является готовым runtime-конфигом и используется как санитизированный шаблон для репозитория.

Исторический Markdown сохранён в `artifacts/legacy-plan.md`; исходный путь legacy-контура зафиксирован в `knowledge/tasks/TASK-2026-0002-plans-to-knowledge-migration/artifacts/legacy/legacy-path-map.md`.
