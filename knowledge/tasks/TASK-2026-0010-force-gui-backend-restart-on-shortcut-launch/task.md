# Карточка задачи TASK-2026-0010

## Паспорт

| Поле | Значение |
|------|----------|
| ID задачи | `TASK-2026-0010` |
| Parent ID | `—` |
| Уровень вложенности | `0` |
| Ключ в путях | `TASK-2026-0010` |
| Технический ключ для новых именуемых сущностей | `—` |
| Краткое имя | `force-gui-backend-restart-on-shortcut-launch` |
| Статус | `завершена` |
| Приоритет | `средний` |
| Ответственный | `не назначен` |
| Ветка | `—` |
| Дата создания | `2026-03-28` |
| Дата обновления | `2026-04-02` |

## Цель

Сделать поведение GUI launcher предсказуемым для desktop-сценариев: каждый запуск через ярлык должен не переиспользовать старый backend, а инициировать его принудительный restart и затем открывать веб-интерфейс уже на новом процессе.

Ожидаемый результат: и portable `subvost-xray-tun.desktop`, и ярлык, установленный через `install-subvost-gui-menu-entry.sh`, всегда запускают `open-subvost-gui.sh` в режиме forced restart backend; при этом ручной запуск `./open-subvost-gui.sh` без специальных аргументов сохраняет текущую модель поведения.

## Подсказка по статусу

Использовать только одно из значений:

- `черновик`
- `готова к работе`
- `в работе`
- `на проверке`
- `ждёт пользователя`
- `заблокирована`
- `завершена`
- `отменена`

## Границы

### Входит

- историческое содержимое legacy-плана, сохранённого в `artifacts/legacy-plan.md`;
- адаптация записи под task-centric структуру `knowledge/tasks/`;
- сохранение исходного Markdown в `artifacts/legacy-plan.md`.

### Не входит

- новая реализация задачи в рамках миграции;
- редактирование исторического смысла legacy-плана;
- удаление или перепридумывание исходного контекста.

## Контекст

- источник постановки: запрос пользователя сделать так, чтобы каждый запуск приложения через ярлык принудительно перезапускал серверную часть
- связанная бизнес-область: legacy-планы проекта
- ограничения и зависимости: миграция выполняется без потери исходного Markdown и без сохранения удалённого legacy-контура как рабочего процесса
- основной контекст сессии: `историческая миграция`

## Затронутые области

| Область | Что меняется |
|---------|--------------|
| Код / сервисы | Историческое описание сохранено в `artifacts/legacy-plan.md` |
| Конфигурация / схема данных / именуемые сущности | Историческое описание сохранено в `artifacts/legacy-plan.md` |
| Интерфейсы / формы / страницы | Историческое описание сохранено в `artifacts/legacy-plan.md` |
| Интеграции / обмены | Историческое описание сохранено в `artifacts/legacy-plan.md` |
| Документация | Созданы `task.md` и `plan.md`, а legacy-план сохранён как артефакт |

## Связанные материалы

- основной каталог задачи: `knowledge/tasks/TASK-2026-0010-force-gui-backend-restart-on-shortcut-launch/`
- файл плана: `plan.md`
- пользовательские материалы: `artifacts/legacy-plan.md`
- связанные коммиты / PR / ветки: `—`
- связанные операции в `knowledge/operations/`, если есть: `—`

## Текущий этап

Историческая задача была завершена до миграции из legacy-контура и перенесена в knowledge-систему как архивная, но доступная запись.

## Стратегия проверки

### Покрывается кодом или тестами

- см. раздел `Проверки` в `plan.md`;
- при необходимости сверить с `artifacts/legacy-plan.md`.

### Остаётся на ручную проверку

- сверить migrated-каталог с `artifacts/legacy-plan.md`; исходный путь при необходимости взять из `knowledge/tasks/TASK-2026-0002-plans-to-knowledge-migration/artifacts/legacy/legacy-path-map.md`.

## Критерии готовности

- каталог `knowledge/tasks/TASK-2026-0010-force-gui-backend-restart-on-shortcut-launch/` содержит адаптированные `task.md` и `plan.md`;
- исходный Markdown сохранён в `artifacts/legacy-plan.md`;
- дальнейшая работа по этой теме, если понадобится, продолжается только из knowledge-системы.

## Итог

Добавлен явный флаг `--force-restart-backend` в `libexec/open-subvost-gui.sh`: при его наличии launcher всегда идёт в `pkexec`-цепочку и вызывает `start-gui-backend-root.sh` с `SUBVOST_GUI_RESTART=1`, не пытаясь переиспользовать уже поднятый backend. При обычном ручном запуске `./open-subvost-gui.sh` прежняя логика совместимого reuse сохранена.

Portable `subvost-xray-tun.desktop` и desktop entry, который генерирует `install-subvost-gui-menu-entry.sh`, теперь оба передают launcher'у `--force-restart-backend`. Это фиксирует требуемое поведение именно для ярлыков: каждый запуск через ярлык приводит к принудительному restart серверной части перед открытием браузера.

README обновлён под новое поведение GUI launcher. Статические проверки `bash -n *.sh`, `bash -n libexec/*.sh`, `bash -n lib/*.sh`, `python3 -m py_compile gui/gui_server.py` и `desktop-file-validate subvost-xray-tun.desktop` прошли без ошибок. Остаточный риск: в этой сессии не выполнялся ручной smoke реального desktop-launch с `pkexec`, поэтому фактический restart backend через системный ярлык нужно подтвердить отдельно на целевой машине.

Исторический Markdown сохранён в `artifacts/legacy-plan.md`; исходный путь legacy-контура зафиксирован в `knowledge/tasks/TASK-2026-0002-plans-to-knowledge-migration/artifacts/legacy/legacy-path-map.md`.
