# Карточка задачи TASK-2026-0054

## Паспорт

| Поле | Значение |
|------|----------|
| ID задачи | `TASK-2026-0054` |
| Parent ID | `—` |
| Уровень вложенности | `0` |
| Ключ в путях | `TASK-2026-0054` |
| Технический ключ для новых именуемых сущностей | `full-ui-redesign` |
| Краткое имя | `full-ui-redesign` |
| Статус | `на проверке` |
| Приоритет | `высокий` |
| Ответственный | `Codex` |
| Ветка | `task/task-2026-0054-full-ui-redesign` |
| Дата создания | `2026-04-11` |
| Дата обновления | `2026-04-11` |

## Цель

Перестроить нативный `GTK4`-интерфейс bundle под фиксированное окно `1280×960`, перевести навигацию на `Gtk.HeaderBar + Gtk.StackSwitcher`, полностью убрать shell-баннер и собрать всю ключевую status-информацию внутри вкладки `Подключение`, сохранив `TASK-2026-0053.6` как fallback-кандидат.

## Подсказка по статусу

Использовать только одно из значений:

- `черновик`
- `готова к работе`
- `в работе`
- `на проверке`
- `ждёт пользователя`
- `заблокирована`
- `завершена`
- `отменена`

## Границы

### Входит

- адаптация всего shell-контракта под фиксированный размер окна `1280×960`;
- замена sidebar-навигации на горизонтальные вкладки в `Gtk.HeaderBar`;
- полное удаление верхнего информационного баннера и перенос status-информации на вкладку `Подключение`;
- переработка экрана `Подключение`: одна status-row, action-row, `Gtk.InfoBar` при конфликте, отдельные блоки `Трафик` и `Интерфейс`, строка дополнительных статусов;
- широкое представление экрана `Узлы и подписки` без дублирования и с улучшенной читаемостью под `1280×960`;
- более подробная, но не перегруженная `Диагностика` с карточками `Сводка / Журнал`;
- финальная полировка `HeaderBar`, кнопок, списков и служебных блоков под целевой размер `1280×960`;
- поэтапная реализация в отдельной ветке без перезаписи `TASK-2026-0053.6`.

### Не входит

- удаление или переписывание fallback-кандидата `TASK-2026-0053.6`;
- смена runtime-модели, `pkexec`-границ или backend-контракта без прямой необходимости для нового UI;
- перевод нового UI в production launcher до отдельного решения и живого smoke;
- обязательная реализация мобильных жестов: сценарий свайпа остаётся опциональным будущим расширением, а не целью первого редизайн-прохода.

## Контекст

- исходный план задачи стартовал как адаптация под `960×540`, затем был поднят до `1280×960` с sidebar-контрактом, а в финальной редакции этой же ветки пользователь закрепил другой shell: фиксированное окно `1280×960`, горизонтальные вкладки в `HeaderBar` и отсутствие отдельного баннера;
- `TASK-2026-0053.6` сохраняется как рабочий запасной `GTK4`-вариант, но теперь сравнивается уже не с ultra-compact сценарием, а с более зрелым layout под `1280×960`;
- новый контур открыт отдельной задачей, потому что речь идёт уже не о точечном UX-pass, а о полноценной смене композиции, навигации и плотности интерфейса;
- под задачу уже создана отдельная git-ветка `task/task-2026-0054-full-ui-redesign`;
- текущий фокус задачи смещён с абстрактного «редизайна вообще» на конкретный проверяемый контракт: фиксированное окно `1280×960`, `HeaderBar + StackSwitcher`, status-row внутри вкладки `Подключение`, переработанные `Узлы и подписки` и упорядоченная `Диагностика`.

## Затронутые области

| Область | Что меняется |
|---------|--------------|
| UI / `GTK4` shell | Переопределяются навигация, layout, плотность, иерархия данных и главные действия под малое окно |
| Python-код native UI | Ожидаются изменения в `gui/native_shell_app.py` и связанных helper-модулях для нового shell-контракта |
| Service-layer интеграция | Возможны локальные правки только там, где новый UI требует иного представления статусов, логов и действий |
| Проверки и smoke | Добавляются целевые проверки под размер `1280×960` и сравнительный smoke с fallback-вариантом |
| Документация | Зафиксированы новый продуктовый контракт, приоритеты реализации и связь с резервным контуром `TASK-2026-0053.6` |

## Связанные материалы

- основной каталог задачи: `knowledge/tasks/TASK-2026-0054-full-ui-redesign/`
- файл плана: `plan.md`
- пользовательский вход для текущей редакции плана: текущий запрос в этой сессии с подробным UI/UX-планом малого окна;
- резервный вариант без перезаписи: `knowledge/tasks/TASK-2026-0053-gtk4-native-ui-spike/subtasks/TASK-2026-0053.6-gtk4-compact-ux-defect-pass/`
- исходный functional spike native UI: `knowledge/tasks/TASK-2026-0053-gtk4-native-ui-spike/`
- рабочая ветка задачи: `task/task-2026-0054-full-ui-redesign`

## Текущий этап

После пользовательского ревью выполнен дополнительный defect-pass по вкладке `Подключение`. Базовый redesign сохранён, но внутри него убраны дубли про чужой экземпляр, action-row оставлен как явный быстрый ряд `Подключиться / Диагностика / Узлы`, в заголовок добавлен контекст активной подписки, `TUN` убран из верхней строки в пользу блока `Интерфейс`, а строка статусов переведена на symbolic-иконки.

Следующий шаг уже не у исполнителя: нужен живой пользовательский smoke на реальном display и контрольное сравнение с fallback-вариантом `TASK-2026-0053.6`.

## Стратегия проверки

### Покрывается кодом или тестами

- для task-контура и документации:
  - адресная проверка локализации Markdown через `markdown-localization-guard`;
  - `git diff --check`;
- для будущих кодовых изменений:
  - `python3 -m py_compile gui/native_shell_app.py gui/native_shell_shared.py gui/subvost_app_service.py gui/gui_server.py`;
  - релевантные `python3 -m unittest` по native-shell и service-layer;
  - `bash -n *.sh`, `bash -n libexec/*.sh`, `bash -n lib/*.sh`.

### Остаётся на ручную проверку

- живой smoke на окне `1280×960`;
- сравнение с `TASK-2026-0053.6` на тех же экранах `Подключение`, `Узлы и подписки`, `Диагностика`, `Settings`;
- подтверждение, что новый свободный layout выглядит целостно и не регрессирует относительно fallback-варианта.

## Критерии готовности

- окно `1280×960` является фиксированным рабочим режимом интерфейса;
- навигация переведена на горизонтальные вкладки в `HeaderBar`, а sidebar полностью удалена;
- экран `Подключение` держит status-row, action-row, `InfoBar` при конфликте и не опирается на отдельный shell-banner;
- экран `Узлы и подписки` не расползается по высоте и ширине из-за длинных URL и повторяющихся кнопок;
- экран `Диагностика` разделяет сводку и журнал, а summary собрана в карточки вместо перегруженного полотна;
- `TASK-2026-0053.6` остаётся сохранённым fallback-вариантом и не теряется в процессе редизайна;
- по новому UI есть как минимум один рабочий vertical slice, который можно прогнать на живом display.

## Итог

В `gui/native_shell_app.py` завершён новый redesign-pass под фиксированное окно `1280×960`. Главное окно теперь работает без sidebar и без отдельного информационного баннера: навигация перенесена в `Gtk.HeaderBar` через `Gtk.StackSwitcher`, заголовок уплотнён по высоте, а размеры окна зафиксированы под один штатный desktop-режим.

Поверх исходного redesign-а сделан отдельный review defect-pass по экрану `Подключение`: конфликт другого экземпляра сведён к одному `Gtk.InfoBar`, основное действие всегда остаётся видимым как кнопка `Подключиться` или `Отключить`, быстрые действия собраны в один явный горизонтальный ряд `Подключиться / Диагностика / Узлы`, а в строку активного узла добавлен контекст подписки вида `подписка: sub.subvost.fun`. `TUN` убран из верхнего заголовка и оставлен только в блоке `Интерфейс` и в строке статусов.

DNS в блоке `Интерфейс` больше не требует дополнительного клика под фиксированное окно `1280×960`: список адресов раскрывается сразу и переносится по строкам внутри карточки. Строка `Статусы` переведена с текстовых маркеров на `Gtk.Image` symbolic-иконки, а блок `Трафик` удерживает явные подписи `Всего`, чтобы убрать артефакты вида `Bzero` и сделать показания читаемыми без двусмысленности.

`Узлы и подписки` сохранены в широкой двухколоночной композиции, а `Диагностика` оставлена карточной: весь дополнительный конфликтный контекст, путь активного bundle и служебные файлы теперь сосредоточены там, а не размазаны по вкладке `Подключение`.

Локально для этого defect-pass прошли `python3 -m py_compile gui/native_shell_app.py` и `python3 -m unittest tests.test_native_shell_app`. Живой `GTK` smoke на реальном display не выполнялся: следующий шаг — пользовательская контрольная проверка этого варианта против ожиданий и, при необходимости, против fallback-кандидата `TASK-2026-0053.6`.
