# Дизайн-ревью Kimi TASK-2026-0061

## Команда

```bash
kimi --work-dir /home/prog7/РабочееПространство/projects/PetProjects/personal-singbox-xray-tun --skills-dir /home/prog7/РабочееПространство/projects/PetProjects/personal-singbox-xray-tun/.agents/skills --quiet --plan -p 'Проведи read-only дизайн-ревью изменений TASK-2026-0061. Смотри текущий git diff и файлы gui/main_gui.html, gui/native_shell_app.py, gui/native_shell_shared.py. Контекст: пользователь хочет верхние вкладки, отдельную вкладку Маршруты, блок Прямые маршруты с русской локализацией, не выносить report на Подписки, Настройки как верхняя вкладка. Нужны только конкретные UX/UI риски и рекомендации, без правок файлов.'
```

## Итог ревью

Kimi нашёл 11 UX/UI рисков:

- высокий: responsive верхних вкладок на промежуточной ширине;
- высокий: пустоватая web-вкладка настроек;
- средний: жёсткая ширина двухпанельного блока `Маршруты`;
- средний: склонение слова `конфликт`;
- средний: термин `Routing-профиль`;
- средний: вкладка `Диагностика` должна иметь диагностическое действие или называться иначе;
- средний: ширина GTK `routes_page`;
- средний: изменение поведения `OpenSettings`;
- низкий: отдельная кнопка `Диагностика` в верхней зоне;
- низкий: empty-state прямых маршрутов без контекстного CTA;
- низкий: дублирование `direct_report` в payload.

## Принятые исправления

- добавлен ранний breakpoint для верхних вкладок;
- web-вкладка `Настройки` упрощена до одной панели;
- действия routing-профиля получили `flex-wrap`;
- добавлена русская плюрализация `конфликт/конфликта/конфликтов`;
- `Routing-профиль` заменён на `Профиль маршрутизации`;
- во вкладку `Диагностика` добавлена кнопка `Снять диагностику`;
- ширина GTK-панелей `Маршруты` уменьшена.

## Оставлено как осознанное решение

- `OpenSettings` теперь открывает вкладку `Настройки`, потому пользователь явно одобрил перенос настроек в верхнюю вкладку;
- `direct_report` оставлен и в корне status, и внутри `routing` для совместимости web/GTK/Windows consumers.

## Ссылка на Kimi session

```text
kimi -r 7cdbd83c-bd6b-4c2c-aa0d-83e6cc37d5b2
```
