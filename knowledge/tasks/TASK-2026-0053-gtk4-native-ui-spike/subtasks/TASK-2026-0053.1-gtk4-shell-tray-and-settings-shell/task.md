# Карточка задачи TASK-2026-0053.1

## Паспорт

| Поле | Значение |
|------|----------|
| ID задачи | `TASK-2026-0053.1` |
| Parent ID | `TASK-2026-0053` |
| Уровень вложенности | `1` |
| Ключ в путях | `TASK-2026-0053.1` |
| Технический ключ для новых именуемых сущностей | `gtk4-shell-tray-settings` |
| Краткое имя | `gtk4-shell-tray-and-settings-shell` |
| Статус | `на проверке` |
| Приоритет | `высокий` |
| Ответственный | `Codex` |
| Ветка | `topic/gtk4-native-ui-spike` |
| Дата создания | `2026-04-09` |
| Дата обновления | `2026-04-09` |

## Цель

Открыть первый реализуемый этап нативного `GTK4`-клиента: собрать каркас desktop-приложения с главным окном, навигацией по трём страницам, системным треем и оболочкой минимальных настроек, не влезая пока в полноценную backend-интеграцию `Dashboard / Subscriptions / Log`.

## Подсказка по статусу

Использовать только одно из значений:

- `черновик`
- `готова к работе`
- `в работе`
- `на проверке`
- `ждёт пользователя`
- `заблокирована`
- `завершена`
- `отменена`

## Границы

### Входит

- создание native-каркаса `GtkApplication` для будущего клиента;
- главное окно `GTK4` с header/navigation и тремя страницами стека:
  - `Dashboard`
  - `Subscriptions`
  - `Log`
- системный трей для Linux-сценария bundle:
  - показать окно;
  - скрыть окно;
  - `Старт`;
  - `Стоп`;
  - `Снять диагностику`;
  - `Выход`;
- fallback-поведение, если в окружении нет рабочего status notifier / системного трея;
- сценарии `close-to-tray` и `start minimized to tray`;
- отдельное минимальное окно настроек как оболочка под `v1`:
  - файловое логирование;
  - закрытие окна в трей;
  - запуск свёрнутым в трей;
  - тема окна;
- запрет на ранний `pkexec` при открытии окна и при открытии настроек.

### Не входит

- service-layer над store/runtime/parser/routing;
- реальная backend-интеграция действий `Старт`, `Стоп` и `Снять диагностику`;
- рабочее наполнение `Dashboard`, `Subscriptions` и `Log`;
- живая routing-секция и блок `GeoIP/Geosite`;
- переключение основного launcher-а проекта на новый native-клиент.

## Контекст

- источник постановки: декомпозиция `TASK-2026-0053` по первому этапу реализации;
- связанная бизнес-область: desktop-оболочка portable Linux bundle для `xray-core + TUN`;
- ограничения и зависимости: каркас должен появиться раньше service-layer и не должен заставлять проект принимать решения о полном отказе от текущего web UI;
- основной контекст сессии: `TASK-2026-0053`.

## Затронутые области

| Область | Что меняется |
|---------|--------------|
| Код / сервисы | Появится каркас нативного `GTK4` приложения и tray-интеграция |
| Конфигурация / схема данных / именуемые сущности | Появятся только UI-shell сущности для окна и минимальных настроек |
| Интерфейсы / формы / страницы | Появится новый native-контур с пустыми страницами `Dashboard / Subscriptions / Log` |
| Интеграции / обмены | Появится Linux-интеграция системного трея с fallback-поведением |
| Документация | Открывается отдельная подзадача и запись в реестре |

## Связанные материалы

- основной каталог подзадачи: `knowledge/tasks/TASK-2026-0053-gtk4-native-ui-spike/subtasks/TASK-2026-0053.1-gtk4-shell-tray-and-settings-shell/`
- файл плана: `plan.md`
- родительская задача: `knowledge/tasks/TASK-2026-0053-gtk4-native-ui-spike/`
- связанные пользовательские требования: системный трей, минимальные настройки, отсутствие раннего `pkexec`
- связанные коммиты / PR / ветки: `—`
- связанные операции в `knowledge/operations/`, если есть: `—`

## Текущий этап

Код реализован и прошёл локальные проверки. Сейчас подзадача находится на этапе проверки: native shell стартует как отдельное `GTK4` приложение, tray-helper работает через `Ayatana AppIndicator` в поддерживаемой сессии, а при отсутствии рабочего status notifier приложение остаётся обычным окном и не перехватывает закрытие. После ревью дополнительно усилен fallback: при аварии tray-helper скрытое окно автоматически возвращается на экран, а режим `system` снова отличается от явного `light`.

## Стратегия проверки

### Покрывается кодом или тестами

- `python3 -m unittest tests.test_native_shell_shared tests.test_native_shell_app tests.test_subvost_store`;
- `python3 -m py_compile gui/native_shell_shared.py gui/native_shell_app.py gui/native_shell_tray_helper.py gui/subvost_store.py`;
- runtime-smoke `GTK4` окна без tray и с tray-helper без раннего `pkexec`;
- проверки fallback-поведения трея в среде без status notifier и совместимого `session D-Bus`.

### Остаётся на ручную проверку

- долгий ручной прогон tray-меню на целевом Linux desktop вне короткого smoke;
- визуальная проверка поведения `close-to-tray` и `start minimized to tray` через реальное взаимодействие с окном;
- открытие окна и настроек с пользовательского launcher-а, а не только через автоматизированный smoke.

## Критерии готовности

- у проекта появляется отдельный native `GTK4` shell-контур;
- системный трей работает в поддерживаемом окружении и имеет безопасный fallback;
- окно настроек существует как минимальная `v1`-оболочка;
- подзадача не затягивает в себя service-layer и остальную бизнес-логику;
- запись в реестре и task-артефакты синхронизированы.

## Итог

Собран первый рабочий native-shell контур:

- `gui/native_shell_app.py` поднимает отдельный `GTK4` `ApplicationWindow`, stack-навигацию `Dashboard / Subscriptions / Log`, статусную панель и окно минимальных настроек;
- `gui/native_shell_tray_helper.py` запускает отдельный `Gtk3 + Ayatana/AppIndicator` helper для Linux tray без смешивания `GTK3` и `GTK4` в одном процессе;
- `gui/native_shell_shared.py` хранит чистую shell-логику, страницы, настройки, tray-actions и fallback-решения, которые покрыты unit-тестами;
- `gui/subvost_store.py` расширен до совместимого формата `gui_settings.json`, чтобы web GUI не затирал поля native shell;
- `libexec/install-on-new-pc.sh` теперь пытается ставить `Gtk3` и `AppIndicator` GI-зависимости там, где они доступны.

Остаточные риски:

- действия `Старт / Стоп / Снять диагностику` пока работают как shell-stub и не подключены к реальному runtime;
- `Subscriptions` и `Log` пока остаются placeholder-страницами без service-layer и живого наполнения;
- tray-контур прошёл короткий runtime-smoke, но нуждается в более длинной ручной проверке на целевом desktop с пользовательским launcher-ом;
- поведение `system`-темы опирается на исходное GTK-предпочтение сессии и должно быть отдельно подтверждено на desktop-окружениях с глобальной тёмной схемой.
