# План задачи TASK-2026-0025

## Правило

Для задачи существует только один файл плана: `plan.md`.
Если задача декомпозируется, каждая подзадача получает свой собственный `plan.md` внутри своей папки.

## Паспорт плана

| Поле | Значение |
|------|----------|
| ID задачи | `TASK-2026-0025` |
| Parent ID | `—` |
| Версия плана | `1` |
| Дата обновления | `2026-04-02` |

## Цель

Сделать локально поддерживаемый сценарий добавления subscription URL и обновления набора узлов по кнопке, чтобы bundle мог сам подтягивать актуальный список нод и сохранять его в operator-managed store.

## Границы

### Входит

- исторический объём legacy-плана, сохранённого в `artifacts/legacy-plan.md`;
- сохранение исходного Markdown в `artifacts/legacy-plan.md`;
- приведение записи к task-centric структуре без потери контекста.

### Не входит

- новая реализация темы во время миграции;
- изменение исторического статуса legacy-задачи.

## Планируемые изменения

### Код

- содержание legacy-плана сохранено без переписывания; детали см. в историческом блоке ниже и в `artifacts/legacy-plan.md`.

### Конфигурация / схема данных / именуемые сущности

- при наличии изменений они описаны в историческом блоке ниже и в `artifacts/legacy-plan.md`.

### Документация

- task-centric карточка и план созданы в `knowledge/tasks/TASK-2026-0025-subscription-url-refresh/`.

## Риски и зависимости

- legacy-план мог использовать ссылки удалённого legacy-контура; они сохраняются как исторический контекст внутри `artifacts/legacy-plan.md`.
- продолжение этой темы, если оно потребуется, должно идти уже из knowledge-системы, а не через восстановление отдельного legacy-контура.

## Проверки

### Что можно проверить кодом или тестами

- при наличии команд и автоматизируемых проверок см. исторический блок `Проверки` ниже.

### Что остаётся на ручную проверку

- сверить migrated-каталог с `artifacts/legacy-plan.md`.

## Шаги

- [x] Перенести исходный legacy-план в task-centric структуру
- [x] Сохранить исходный Markdown в `artifacts/legacy-plan.md`
- [x] Историческое состояние задачи зафиксировано и не требует дополнительной миграционной декомпозиции

## Критерии завершения

- Историческая задача уже завершена; текущий критерий миграции — сохранить контекст без потери деталей.

## Источник legacy-плана

- текущий артефакт: `artifacts/legacy-plan.md`
- карта миграции исходного пути: `knowledge/tasks/TASK-2026-0002-plans-to-knowledge-migration/artifacts/legacy/legacy-path-map.md`
- исходный статус: `done`
- исходный заголовок: `Импорт и обновление подписок по URL`

## Исторический контекст

### Изменения

- Ввести локальное хранилище подписок:
  - display name;
  - URL подписки;
  - время последнего успешного обновления;
  - метаданные ошибки последнего обновления;
  - ETag / Last-Modified, если upstream это поддерживает.
- Реализовать fetch/update слой в Python:
  - ручное обновление одной подписки;
  - ручное обновление всех подписок;
  - понятное состояние `успешно / частично / ошибка`.
- Поддержать основные форматы ответа:
  - base64-encoded список ссылок;
  - plain-text список ссылок построчно;
  - fallback-отказ с диагностикой, если upstream-format не распознан.
- После обновления переводить контент подписки в ту же нормализованную модель узлов, что и импорт одиночных ссылок.
- Ввести dedup между узлами:
  - по стабильному fingerprint;
  - без молчаливой потери пользовательских меток и ручных переименований.
- Добавить GUI-действия:
  - `Добавить подписку`;
  - `Обновить`;
  - `Обновить все`;
  - `Показать ошибку обновления`;
  - `Отключить / удалить подписку`.

## Исторические проверки

- Smoke с санитизированной тестовой подпиской, которая возвращает несколько ссылок.
- Негативные сценарии: таймаут, `404`, битый base64, пустой ответ, дублирующиеся ноды, частично невалидные строки.
- Ручной smoke: обновить подписку, увидеть новые узлы в UI, выбрать активный узел без правки `xray-tun-subvost.json`.

## Исторические допущения

- В первой итерации не нужен фоновый auto-refresh daemon; достаточно явного обновления пользователем.
- Subscription URL остаётся локальными operator-managed данными и не попадает в Git, планы и README.
- Этот черновик вместе с импортом одиночных ссылок образует первую обязательную волну доработок.

## Исторический итог

- Добавлено локальное хранилище подписок с `display name`, URL, статусом последнего обновления, `ETag` и `Last-Modified`.
- Реализованы ручное обновление одной подписки и `Обновить все`, распознавание `plain-text` и `base64`-ответов, а также сохранение последней ошибки обновления в store и UI.
- Обновление подписки переводит ссылки в общую нормализованную модель узлов, выполняет dedup по fingerprint и сохраняет пользовательские переименования узлов.
- Локально подтверждены unit-тесты и статические проверки; ручной smoke с реальной сетевой подпиской в этой сессии не выполнялся и остаётся остаточным риском.
