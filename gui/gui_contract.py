from __future__ import annotations

# GUI contract version must change on incompatible routing or API expectations
# so launcher restart checks can detect stale backends after bundle updates.
GUI_VERSION = "2026-04-08-main-gui-live-asset-v1"
